module ArrayParamsModule {
    func sumFirstTwo(int[] xs) returns int {
        return xs[0] + xs[1]
    }

    func asDecArray(int[] xs) returns dec[] {
        return xs castas dec[]
    }

    func firstBool(bool[] flags) returns bool {
        return flags[0]
    }
}
