// module FibonacciModule
function fib(n) {
    if ((n < 2)) {return n;}
    return (fib((n - 1)) + fib((n - 2)));
}

function sumOfFibs(upTo) {
    let total = 0;
    let i = 0;
    while ((i < upTo)) {total += fib(i);
i++;}
    return total;
}

